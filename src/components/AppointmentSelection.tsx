import SectionTitle from 'components/SectionTitle';

const SelectionContent = () => <>Placeholder</>;

const AppointmentSelection = (): JSX.Element => (
  <>
    <SectionTitle>New appointment</SectionTitle>
    <SelectionContent />
  </>
);

export default AppointmentSelection;
