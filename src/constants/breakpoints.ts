const TABLET_SIZE_PX = 768;
export const TABLET_ONLY = `@media only screen and (max-width: ${TABLET_SIZE_PX}px)`;
export const DESKTOP_ONLY = `@media only screen and (min-width: ${TABLET_SIZE_PX}px)`;
